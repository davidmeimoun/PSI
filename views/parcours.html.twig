{% extends "layout.html.twig" %}

{% block title %}Home{% endblock %}

{% block content %}
  {% for parcours in parcoursList %}
        <parcours>
          <h2><a class="coursTitle" href="{{ path('ue', { 'id': parcours.id }) }}">{{ parcours.libelle }}</a></h2>
        </parcours>
        {% endfor %}
{% endblock %}